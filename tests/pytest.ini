[pytest]
addopts = -s

#指定用例搜索目录
testpaths = tests

# 忽略warning
;addopts = -p no:warnings

# mark
markers =
    test_plat: category

;log_cli: 1

filterwarnings =
    ignore::UserWarning
    ignore:.*U.*mode is deprecated:DeprecationWarning